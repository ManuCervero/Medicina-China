{
  "name": "carolina-escudero-medicina-china",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "npm --prefix backend run dev",
    "db:migrate": "npm --prefix backend run db:migrate:local"
  }
}
